# Copy to .env (gitignored). Referenced from config.yaml as ${VAR}.

# Email delivery (Gmail: use an App Password, not your account password)
DIGEST_EMAIL=you@example.com
SMTP_USERNAME=you@example.com
SMTP_PASSWORD=
SMTP_HOST=smtp.gmail.com

# Slack delivery
SLACK_WEBHOOK_URL=
